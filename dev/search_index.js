var documenterSearchIndex = {"docs":
[{"category":"section","location":"models/elevator/debugging/#Debugging-with-the-Elevator","page":"Debugging Tools","text":"The elevator is the main fixture for ChronoSim's \"why-verb\" debugging tools, tested in test/test_whyverbs.jl. Those tests are more involved than a smoke run, and they double as worked examples of the questions you can ask a simulation when it does not do what you expect: why did this event never fire?, why was it rejected?, and why did the run stop?\n\nThe scenarios below use a larger configuration than run_elevator — three people, two elevators, five floors — built directly rather than through the entry point:\n\n_elev_events(M) = [M.PickNewDestination, M.CallElevator, M.OpenElevatorDoors,\n    M.EnterElevator, M.ExitElevator, M.CloseElevatorDoors, M.MoveElevator,\n    M.StopElevator, M.DispatchElevator]\n\nP, E, F, seed = 3, 2, 5, 93472934\nsim = SimulationFSM(_ED.ElevatorSystem(P, E, F), _elev_events(_ED);\n    sampler=NextReactionMethod(), key_type=Tuple, rng=Xoshiro(seed),\n    policy=RecordSkeleton())\nChronoSim.run(sim, _ED.init_physical, (p, i, e, w) -> w > 120.0)\n\nRecordSkeleton is a policy that records a lightweight trace of the run so the diagnostic tools have a history to point at.","title":"Debugging with the Elevator"},{"category":"section","location":"models/elevator/debugging/#whynot:-an-event-that-never-fired","page":"Debugging Tools","text":"The first scenario asks why StopElevator was never even proposed. It uses a deliberately broken fixture, ElevatorStopBug (in test/whyverbs_elevator_stopbug.jl), a copy of the hand-written model with the extra StopElevator triggers reverted — exactly the bug the derived twin once exposed. Because StopElevator is missing a trigger, no state change ever proposes it, and whynot reports it as :never_proposed, pointing at the missing dependency.","title":"whynot: an event that never fired"},{"category":"section","location":"models/elevator/debugging/#whynot:-an-event-proposed-but-rejected","page":"Debugging Tools","text":"The second scenario uses ElevatorNoButton (in test/whyverbs_elevator_nobutton.jl), a copy of the derived model with the button-press clause deleted from EnterElevator's precondition. Here the event is proposed but its precondition returns false, so whynot reports :rejected and breaks the precondition down clause by clause — showing that call_exists || button_pressed failed because the button branch was removed.\n\nThe contrast between the two scenarios is the point: :never_proposed means the framework never considered the event (a missing trigger), while :rejected means it considered it and the precondition said no (a logic error). The debugging tool distinguishes the two so you know whether to look at your triggers or your precondition.","title":"whynot: an event proposed but rejected"},{"category":"section","location":"models/elevator/debugging/#whystopped:-an-invariant-violation-names-the-culprit","page":"Debugging Tools","text":"The last scenario shows how a violated invariant is traced back to the event that caused it. A test-local CorruptPerson event is added that deliberately breaks the \"person location xor elevator\" invariant. The run stacks two policies — recording and invariant checking — so that when the invariant fails, the error carries a replayable prefix:\n\npolicy = PolicyStack(RecordSkeleton(), CheckInvariants(ElevatorExample))\n\nWhen the CorruptPerson event fires and the invariant fails, ChronoSim.run throws an InvariantViolation. Passing that error to whystopped names the guilty writer, the clock that fired, and the command to replay the run up to the violation:\n\ntry\n    ChronoSim.run(sim, _ED.init_physical, stop_condition)\ncatch err\n    whystopped(err)   # names the event, the guilty address, and a replay command\nend","title":"whystopped: an invariant violation names the culprit"},{"category":"section","location":"models/elevator/debugging/#Why-this-lives-in-the-examples","page":"Debugging Tools","text":"These debugging tools are ChronoSim features, not elevator features — but the elevator is the model complex enough to demonstrate them honestly. Nine interacting event types, precondition recursion, and a hand-vs-derived twin give the tools real bugs to diagnose. The deliberately broken fixtures (ElevatorStopBug, ElevatorNoButton) are full copies of the model with one line changed, which is how you can see the tool point at exactly that line.","title":"Why this lives in the examples"},{"category":"section","location":"possible/#Possible-Simulations","page":"Other Simulations","text":"There are common simulation targets.\n\nManufacturing - flow of materials and products like the job-shop scheduling problem (JSSP).\nLogistics and transportation - traffic flow, congestion.\nHealthcare systems - ERs, ambulances.\nComputer networks.\nInventory management - classic.\nFinancial modeling - transaction processing, risk management.\nFlight simulators, autopilot.\nGame engines.","title":"Possible Simulations"},{"category":"section","location":"models/reliability/model/#The-Reliability-Model","page":"The Model","text":"The reliability example simulates a fleet of machines — think of a yard of trucks — that go out to work each day and, as they accumulate hours, break down and get repaired. It is a compact model whose real job in this repository is to demonstrate that ChronoSim's older three-argument enable signature still works unchanged.","title":"The Reliability Model"},{"category":"section","location":"models/reliability/model/#Where-it-comes-from","page":"The Model","text":"There is no external citation. The model is a purpose-built example. Its vocabulary is generic — the code speaks of actors, workers, and a crew rather than trucks specifically — so read \"machine\" for any unit that works, ages, and fails.","title":"Where it comes from"},{"category":"section","location":"models/reliability/model/#What-it-models","page":"The Model","text":"A fixed fleet of 15 machines. Each day a crew of up to 10 of them is put to work at a fixed start time (8:00, with time measured in days). While a machine is working it can either finish its day or break down. The chance of breaking down rises with the machine's accumulated work age — the total time it has ever spent working — so machines that have logged more hours fail sooner. A broken machine is repaired, which returns it to service and resets its age to zero, as though it were made new.","title":"What it models"},{"category":"section","location":"models/reliability/model/#State","page":"The Model","text":"Each machine is a keyed record:\n\n@enum Activity ready working broken\n\n@keyedby Individual Int64 begin\n    state::Activity                 # ready, working, or broken\n    work_age::Float64               # cumulative time ever spent working\n    started_working_time::Float64   # sim time the current stint began\nend\n\nEach machine also carries an immutable bundle of the distributions that govern its timing:\n\nstruct IndividualParams\n    done_dist::LogUniform    # time to finish the workday\n    fail_dist::LogNormal     # time to failure\n    repair_dist::Weibull     # repair duration\nend\n\nThe whole fleet is the observed physical state:\n\n@observedphysical IndividualState begin\n    actors::ObservedVector{Individual,Member}\n    params::Vector{IndividualParams}\n    workers_max::Int          # daily crew-size cap\n    start_time::Float64       # time of day the workday starts (8/24)\nend\n\nThe constructor IndividualState(actor_cnt, crew_size) builds every machine as Individual(ready, 0.0, 0.0) and gives them one shared parameter set: LogUniform(0.8, 0.99) for finishing the day, LogNormal(1.5, 0.4) for failure, and Weibull(1.0, 2.0) for repair.\n\n(Image: Clock distributions)\n\nThe three clocks live on very different timescales: the end-of-day LogUniform is a narrow spike just under one day, while failure and repair spread across several days.","title":"State"},{"category":"section","location":"models/reliability/model/#Events","page":"The Model","text":"Four events drive the model. StartDay has no argument; the others carry a machine index.\n\n(Image: Machine state machine)\n\nA single machine cycles through three states; each arrow is the event that drives the transition.\n\nStartDay — always enabled, it fires once per day at the workday start. Its enable computes a deterministic delay to the next 8:00 boundary and returns a Dirac clock, so the workday begins at the same time every day. When it fires, it walks the fleet and sets ready machines to working (recording their start time) until the crew cap is reached.\n\nEndDay(machine) — enabled while a machine is working. Its clock is the machine's done_dist (LogUniform). Firing returns the machine to ready and adds the elapsed stint to its work_age.\n\nBreak(machine) — enabled while a machine is working. This is where the aging model lives. Rather than starting the failure clock at the current stint, its enable offsets the clock's start backward by the accumulated work age:\n\nfunction enable(evt::Break, physical, when)\n    started_ago = when - physical.actors[evt.actor_idx].work_age\n    return (physical.params[evt.actor_idx].fail_dist, started_ago)\nend\n\nso the failure hazard is a function of total work age, not of the current day. Firing sets the machine broken and folds the stint into work_age.\n\nRepair(machine) — enabled while a machine is broken. Its clock is the repair_dist (Weibull). Firing returns the machine to ready and resets work_age = 0.0, restoring it to as-new condition.\n\nA warm-start initialize! gives each machine a random initial work_age drawn from Uniform(0, 10), so the fleet does not all age in lockstep.","title":"Events"},{"category":"section","location":"models/reliability/model/#The-three-argument-enable,-on-purpose","page":"The Model","text":"Notice that the enable methods above take three arguments — enable(event, physical, when) — not the four-argument form enable(event, physical, θ, when) that the landspread model uses. This is deliberate. The header comment explains:\n\nThis module DELIBERATELY keeps the pre-θ-seam three-argument enable signature. ChronoSim's engine now calls the four-argument θ-seam form, and its default method forwards to this one, so a model that reads no parameters from θ needs no change. This module (and its ReliabilityDerivedSim twin) is the suite's standing evidence that the fallback works.\n\nIn other words, the engine always calls the four-argument form internally; a default method forwards it to a three-argument method. A model that reads nothing from the parameter vector never has to be touched. Reliability exists to prove that backward-compatibility path keeps working.","title":"The three-argument enable, on purpose"},{"category":"section","location":"models/reliability/model/#The-derived-twin","page":"The Model","text":"As with the other paired models, ReliabilityDerivedSim (src/reliability/reliability_derived.jl) is a copy whose triggers are derived from the preconditions. The physical state, events, and timing are identical; the only difference is that EndDay, Break, and Repair declare a @precondition and let ChronoSim derive their triggers, instead of writing @conditionsfor blocks by hand. StartDay stays hand-written in both twins, because its precondition is true — it reads no state, so there is nothing to derive from.\n\nBecause both twins key their clocks by nameof(type), their trajectories can be compared directly; that comparison is what test/test_differential.jl and test/test_overapprox.jl check. See Running the Reliability Model.","title":"The derived twin"},{"category":"section","location":"models/sirvillage/model/#The-SIR-Village-Model","page":"The Model","text":"SIR Village is an individual-based epidemic simulation with a twist: the pathogen itself evolves. People move around a village, catch and recover from disease at rates that differ from person to person, and the disease mutates into new strains that escape prior immunity. It is the largest and most feature-rich model in the repository.","title":"The SIR Village Model"},{"category":"section","location":"models/sirvillage/model/#Where-it-comes-from","page":"The Model","text":"There is no cited paper; the origin is a design narrative in the module docstring. The author's stated goal:\n\nThe goal is to make a disease simulation that's a little bit fun. You may have seen disease simulations that show susceptible-infectious-recovered (SIR) transitions for individuals. That's easy to do without any framework, so let's make a halfway realistic simulation...\n\nFour design choices give the model its character:\n\nGamma recovery. Recovery time is Gamma-distributed — \"the classic rate use in disease studies, not Exponential.\"\nHeterogeneous susceptibility. Each person has a different robustness; \"epidemiologists have long understood this is crucial for understanding disease spread.\"\nLocalized contact through movement. People \"move around a city, or let's say a village, so that they have localized sets of contacts. Each person follows a semi-Markovian walk around a subset of locations.\" That movement model is derived in Characterizing Travel.\nAn evolving pathogen. Credited to a colleague who \"railed at me for not including evolution in SIR simulations\" — the disease mutates into strains with different infectivity and virulence, and \"every new strain escapes the previous one, so it can reinfect a person.\"","title":"Where it comes from"},{"category":"section","location":"models/sirvillage/model/#What-it-models","page":"The Model","text":"An agent-based SIRS epidemic with pathogen evolution, set in a village. Individuals move among discrete locations; disease transmits only between people who share a location. The three disease states are Susceptible, Infectious, and Recovered — and because a Reset event returns recovered people to susceptible, immunity wanes and the model is SIRS rather than plain SIR. There is no latent compartment, so it is not SEIR.\n\n(Image: SIRS compartment cycle with the driving events on the arrows)\n\nThe three disease states and the events that move a person between them; Mutate acts within Infectious, giving birth to a child strain.\n\nEvery individual is tracked separately with its own parameters: a robustness draw that governs susceptibility and recovery speed, a personal subset of locations it visits, and personal dwell and exit parameters. The pathogen is a growing set of strains, each with an infectivity and a virulence, that are born inside infectious people and can reinfect those who recovered from an ancestor strain.","title":"What it models"},{"category":"section","location":"models/sirvillage/model/#State","page":"The Model","text":"Individuals carry their disease state, current strain, and current location:\n\n@enum DiseaseState Susceptible Infectious Recovered\n\n@keyedby Individual Int64 begin\n    state::DiseaseState\n    strain::Int64      # infecting strain id; 0 if none\n    haunt::Int64       # index into this person's haunts, not a global location id\nend\n\nPer-person configuration is an immutable struct, never mutated after construction:\n\nstruct IndividualParams\n    robustness::Float64\n    haunts::Vector{Int64}       # global location ids this person visits\n    exit_sum::Vector{Float64}   # per-haunt Weibull rate constant (dwell time)\n    exit_frac::Vector{Float64}  # per-haunt destination probability\nend\n\nLocations track who is present, and strains form a small phylogeny:\n\n@keyedby Location Int64 begin\n    individual_cnt::Int64\n    individuals::Vector{Int64}   # roster of ids currently here\nend\n\n@keyedby Strain Int64 begin\n    infectivity::Float64   # scales infection rate\n    virulence::Float64     # scales recovery time\n    parent::Int64          # parent strain id; 0 for the founding strain\nend\n\nThe whole village is the observed physical state:\n\n@observedphysical Village begin\n    actors::ObservedVector{Individual,Member}\n    actor_params::Param{Vector{IndividualParams}}   # immutable config, unobserved\n    locations::ObservedVector{Location,Member}\n    strains::ObservedDict{Int64,Strain,Member}\n    next_strain_id::Int64\nend\n\nTwo modeling decisions here are worth calling out, because they were driven by the framework:\n\nstrains is an ObservedDict, not an ObservedVector. Strains are born at runtime, and a fixed-extent vector throws FixedExtentError on push!. A keyed dict grows safely; next_strain_id hands out fresh ids without reading the dict's length inside an event body. (Before this change, any run long enough to mutate would crash — see the usage page.)\nactor_params is wrapped in Param{...} to mark it unobservable. As a plain vector, reads of it would emit whole-field read notifications that the derivation coverage oracle would flag.","title":"State"},{"category":"section","location":"models/sirvillage/model/#Events","page":"The Model","text":"Six event types drive the simulation, plus a bootstrap InitEvent.\n\nTravel(who) — always enabled; a rate-driven walk. Its clock is Weibull(2, scale) with scale = 2 / exit_sum[haunt], the linearly-rising hazard from the movement model. Firing chooses a destination haunt from a Categorical over exit_frac, updates the source and destination location rosters, and sets the new haunt.\n\nInfect(source, sink) — enabled when the source is Infectious, the sink is Susceptible, and both are at the same location. Its clock is Exponential(inv(infectivity / robustness)), so a more robust sink or a less infective strain means a longer wait. Firing copies the source's strain to the sink and makes it Infectious.\n\nRecover(who) — enabled while a person is Infectious. Its clock is the classic Gamma(3, inv(robustness / virulence)); a more virulent strain lengthens recovery. Firing moves the person to Recovered.\n\nReset(who) — enabled while a person is Recovered. Its clock is Exponential(inv(0.05)) (mean 20 time units). Firing returns the person to Susceptible with strain 0 — this is the \"S\" that makes the model SIRS.\n\nMutate(carrier) — enabled while a person is Infectious; \"mutation happens within the sick person.\" Its clock is Exponential(inv(0.01)). Firing draws a child strain's infectivity and virulence from a bivariate lognormal centered on the parent's values, creates a new Strain with parent set to the current strain, assigns it next_strain_id, and switches the carrier to the new child. This is the runtime \"birth\" that requires the dict-keyed strains.\n\n(Image: Strain phylogeny built from a 30-person, 15-day run)\n\nThe parent field of each Strain forms a tree rooted at the founding strain (id 1). Node color is infectivity, node size is virulence; both drift from the parent's values through the bivariate-lognormal mutation kernel.\n\nInitEvent() — the bootstrap. It places each actor at a random haunt, infects the first ten percent of the population with the founding strain, and seeds a few extra strains by directly firing Mutate a handful of times.","title":"Events"},{"category":"section","location":"models/sirvillage/model/#Invariants","page":"The Model","text":"The model declares @invariant blocks that a CheckInvariants policy verifies after every event: there is always at least one strain, strain rates are nonnegative, every strain's parent id is valid, each location's count matches its roster, and every sick actor carries a strain. The usage page shows how to run with these checks enabled.","title":"Invariants"},{"category":"section","location":"models/sirvillage/model/#The-derived-twin","page":"The Model","text":"SIRVillageDerived (src/sirvillage/sirvillage_derived.jl) is the derived-generator twin. The physical state, events, and timing are identical; the difference is that Infect, Recover, Reset, and Mutate have their hand-written trigger blocks deleted and let ChronoSim derive triggers from the precondition. Travel stays hand-written in both twins, because its precondition is true and reads no state.\n\nInfect is the headline case. Its precondition reads actors[source].state/.haunt and actors[sink].state/.haunt, so a single change to one person's state fires two derived generators — that person as a source with every possible sink, and as a sink with every possible source — proposing on the order of N candidate infections per change. The precondition's source_loc == sink_loc clause then filters those over-proposals back to exactly the co-located pairs the hand-written generator would have produced. This over-approximate-then-filter behavior is measured in test/test_overapprox.jl.","title":"The derived twin"},{"category":"section","location":"models/repairshop/gradients/#Gradient-Estimators","page":"Gradient Estimators","text":"The likelihood workflow differentiates the log-likelihood of a single fixed trace. The companion example, src/repairshop/repairshop_gradients.jl, tackles a harder problem: estimating the gradient of an expectation over trajectories — here, the derivative in θ of the expected number of down machines at a horizon time. It runs ClockGradients' whole estimator family against one closed-form oracle, on the same simple law.","title":"Gradient Estimators"},{"category":"section","location":"models/repairshop/gradients/#The-closed-form-oracle","page":"Gradient Estimators","text":"Because the machines are independent two-state chains, the expected number down at time T, starting all-up, has a closed form:\n\nexpected_down(θ, machine_cnt, horizon) =\n    machine_cnt * (λ / (λ + μ)) * (1 - exp(-(λ + μ) * horizon))   # λ, μ = θ[1], θ[2]\n\nexpected_down_gradient(θ, machine_cnt, horizon) =\n    ForwardDiff.gradient(p -> expected_down(p, machine_cnt, horizon), θ)\n\nEvery estimator below is checked against expected_down_gradient. The test suite first cross-checks the oracle itself against a finite difference, so the reference is trustworthy before anything is compared to it.","title":"The closed-form oracle"},{"category":"section","location":"models/repairshop/gradients/#The-pure-model-twin","page":"Gradient Estimators","text":"Two of the estimators need to replay records and speculatively fire pairs of events, so they require the model expressed as a pure ClockGradients \"law\" — five functions (initial_state, clockkeytype, enabled, clock_distribution, fire) over an explicit state — rather than the observed-physical ChronoSim model. That twin is ShopLaw, whose state is just a boolean down-vector:\n\nstruct ShopLawState\n    down::Vector{Bool}\nend\nBase.:(==)(a::ShopLawState, b::ShopLawState) = a.down == b.down\n\nThe custom == matters: SPA's criticality gate recognizes commuting event swaps by comparing states by value, and the default struct equality is identity, which would silently disable the gate. The twin's clock keys deliberately match ChronoSim's convention — (:Breakdown, m) and (:Repair, m) — and the estimator audits the twin's enabled set against the live simulation at every step, stopping with a named error on the first disagreement so the hand-written duplication cannot drift.","title":"The pure-model twin"},{"category":"section","location":"models/repairshop/gradients/#The-estimators","page":"Gradient Estimators","text":"The repair_shop_gradients_demo runs all four (oracle plus three estimators) and prints each against the oracle. Each estimator illustrates a different member of the family and a different lesson.\n\n(Image: Estimator family compared to the closed-form oracle, per parameter component)\n\nPer parameter component, the score (pairing), branching, and SPA estimates with ±1 standard-error bars bracket the closed-form oracle (diamond); nreps = 800, horizon = 8.0, θ = [0.3, 1.0]. SPA's wider bars reflect the caveat below — it has no variance advantage on this order-insensitive functional.\n\nScore / IPA pairing — repair_shop_pairing. Runs the score-function and pathwise (IPA) estimators together on the pure model. For a terminal down-count, the pathwise estimate is identically zero — the count is a frozen discrete read that pathwise differentiation cannot see — while the score estimator recovers the oracle. The pairing exists precisely to flag that the IPA estimate is biased here:\n\n@test pair.ipa[j] == 0.0                                    # frozen discrete read\n@test abs(pair.score[j] - oracle[j]) < 4 * pair.score_stderr[j]\n@test pair.score_stderr[j] < abs(oracle[j]) / 5             # standard error bites\n@test any(pair.bias_detected)\n\nBranching estimator — repair_shop_branching. The weak-derivative branching estimator, driven through the live ChronoSim simulation via the ClockGradients–ChronoSim extension. It matches the oracle within four standard errors:\n\n@test abs(br.estimate[j] - oracle[j]) < 4 * br.stderr[j]\n@test br.stderr[j] < abs(oracle[j]) / 4\n\nSmoothed perturbation analysis (SPA) — repair_shop_spa. Runs through the live simulation with ShopLaw as the pure-model twin it audits. SPA's criticality gate asks whether swapping the order of two events would change the outcome; on independent machines every swap is harmless, so the gate proves every swap commutes, spawns no clones, and the whole derivative flows through its horizon boundary term (machines crossing into the observation window). The structural diagnostics make that visible:\n\n@test spa.skip_fraction == 1.0     # every swap proven harmless\n@test spa.clones_per_rep == 0.0    # so no clones were spawned\n@test spa.ipa_part == [0.0, 0.0]   # the derivative is all boundary term","title":"The estimators"},{"category":"section","location":"models/repairshop/gradients/#An-honest-caveat","page":"Gradient Estimators","text":"The SPA docstring is candid that this model does not play to SPA's strengths: with no order effects, SPA has no variance advantage here, and the plain score estimator is several times tighter per replication. SPA earns its keep on order-carried functionals — queueing counts, contended first-passage times — not on independent machines. The example includes it anyway because the point is to show the whole family on a law simple enough to check exactly, including where each member is and is not the right tool.","title":"An honest caveat"},{"category":"section","location":"models/repairshop/gradients/#The-tests","page":"Gradient Estimators","text":"test/test_repairshop_gradients.jl is a statistical suite following what its header calls the \"four standard-error convention\": fixed seeds, agreement within four standard errors, plus a standard-error smallness guard so the comparison actually bites. Its four testsets are (1) the oracle self-check against a finite difference, (2) the pairing flagging the pathwise zero while the score recovers the oracle, (3) the branching estimator matching the oracle, and (4) SPA proving every swap harmless and carrying the derivative in its horizon term.\n\nThe result objects the estimators return are worth knowing when you read the tests: pairing exposes .ipa, .score, .score_stderr, and .bias_detected; branching exposes .estimate, .stderr, and .clones_per_rep; SPA exposes .estimate, .stderr, .skip_fraction, .clones_per_rep, and .ipa_part.","title":"The tests"},{"category":"section","location":"models/elevator/running/#Running-the-Elevator","page":"Running It","text":"This page shows how to build and run the elevator simulation, how to turn on invariant checking, and where the model is tested.","title":"Running the Elevator"},{"category":"section","location":"models/elevator/running/#The-entry-point","page":"Running It","text":"The hand-written model exposes run_elevator, which wires up a small scenario — one person, one elevator, three floors, run for 120 simulated minutes — and returns the final simulation time:\n\nfunction run_elevator(; policy=ChronoSim.NoPolicy())\n    person_cnt, elevator_cnt, floor_cnt = 1, 1, 3\n    minutes = 120.0\n    physical = ElevatorSystem(person_cnt, elevator_cnt, floor_cnt)\n    included_transitions = [\n        PickNewDestination, CallElevator, OpenElevatorDoors, EnterElevator,\n        ExitElevator, CloseElevatorDoors, MoveElevator, StopElevator,\n        DispatchElevator,\n    ]\n    trajectory = TrajectorySave()\n    sim = SimulationFSM(\n        physical, included_transitions;\n        sampler=NextReactionMethod(), key_type=ClockKey,\n        rng=Xoshiro(93472934), observer=trajectory, policy=policy)\n    stop_condition = (physical, step_idx, event, when) -> when > minutes\n    ChronoSim.run(sim, init_physical, stop_condition)\n    return sim.when\nend\n\nThe shape here is the same one every model in this repository follows:\n\nBuild the observed physical state.\nList the event types the simulation may fire (included_transitions).\nConstruct a SimulationFSM with a sampler, a random seed, and — optionally — an observer and a policy.\nDefine a stop_condition, which sees each event before it fires so a time boundary is exact.\nCall ChronoSim.run(sim, initializer, stop_condition).\n\nThe initializer runs at time zero and must write the state that events depend on, because those first writes are what propose the first candidate events. For the elevator it randomizes each person's starting floor:\n\nfunction init_physical(physical, when, rng)\n    for who in eachindex(physical.person)\n        floor = rand(rng, 1:physical.floor_cnt)\n        physical.person[who] = Person(floor, floor, 0, false)\n    end\nend\n\nTo run it from the package:\n\nusing ChronoSimExamples.ElevatorExample\nElevatorExample.run_elevator()","title":"The entry point"},{"category":"section","location":"models/elevator/running/#Watching-the-trajectory","page":"Running It","text":"The observer argument is a callable the framework invokes after every firing. The model's TrajectorySave observer records each fired event's clock key and time, and (in this model) prints the building state so you can watch cars and people move:\n\nstruct TrajectorySave\n    trajectory::Vector{TrajectoryEntry}\nend\n# called as observer(physical, when, event, changed_places) after each firing\n\nAn observer is the intended way to pull data out of a run — nothing else needs to know the event types. Capturing the car's floor, its door state, and the rider's position at every firing yields the trajectory of a single 120-minute run:\n\n(Image: Elevator car floor over a 120-minute run) The step line is the single car's floor; diamonds mark door openings, triangles mark the rider boarding and exiting. The rider's floor (orange) tracks the car while riding and rests on a floor while idle.\n\nCounting each firing by its event type over the same run shows how the work is distributed: doors open and close most often, movement and dispatch next, and the rider's own decisions least.\n\n(Image: How often each event fired during the run) Event firings tallied by clock_key(event)[1] across the 120-minute run.","title":"Watching the trajectory"},{"category":"section","location":"models/elevator/running/#Turning-on-invariant-checking","page":"Running It","text":"The policy keyword is where ChronoSim's checking machinery plugs in. Passing CheckInvariants(ElevatorExample) verifies every @invariant in the module after each fired event and stops with a named violation if one fails:\n\nusing ChronoSimExamples.ElevatorExample\nusing ChronoSim: CheckInvariants\n\nElevatorExample.run_elevator(policy=CheckInvariants(ElevatorExample))\n\nInvariant checking is off by default (the default policy=ChronoSim.NoPolicy()), because it costs time on every step; you turn it on while developing or debugging.","title":"Turning on invariant checking"},{"category":"section","location":"models/elevator/running/#The-smoke-test","page":"Running It","text":"The dedicated test, test/test_elevator.jl, is a single end-to-end smoke test. It runs the whole 120-minute simulation with invariant checking on and asserts that the run advanced:\n\n@testset \"Elevator smoke\" begin\n    using ChronoSimExamples.ElevatorExample\n    using ChronoSim: CheckInvariants\n    with_logger(ConsoleLogger(stderr, Logging.Debug)) do\n        run_duration = ElevatorExample.run_elevator(policy=CheckInvariants(ElevatorExample))\n        @assert run_duration > 9.9\n    end\nend\n\nThat short test carries real weight: with the CheckInvariants policy, a run that completes has also verified all eight safety invariants after every one of its events. If any elevator action ever produced an illegal state — a passenger both on a floor and in a car, a call with no one waiting — the run would stop and name the invariant and the guilty address.\n\nThe elevator is exercised much more thoroughly outside this file. The derived twin is compared against the hand-written model in test/test_differential.jl and test/test_overapprox.jl, model-checked through Quint in test/test_quint.jl, and used as the main fixture for ChronoSim's debugging tools in test/test_whyverbs.jl. That last set is rich enough to deserve its own page: see Debugging with the Elevator.","title":"The smoke test"},{"category":"section","location":"models/landspread/usage/#Running-the-Landspread-Model","page":"Running It","text":"Landspread has two entry points: one that generates a trajectory, and one that scores a trajectory's log-likelihood at a chosen θ. Together they demonstrate the whole point of the θ seam — generate once, score at many parameter values.","title":"Running the Landspread Model"},{"category":"section","location":"models/landspread/usage/#Generating-a-trajectory","page":"Running It","text":"run_landspread(point_cnt; θ=SPREAD_THETA) builds a landscape, runs the spread to saturation, and returns the recorded trajectory:\n\nfunction run_landspread(point_cnt; θ=SPREAD_THETA)\n    rng = Xoshiro(9437294723)\n    land = Landscape(point_cnt, rng)\n    trajectory = TrajectorySave()\n    sim = SimulationFSM(land, [Spread]; rng=rng, observer=trajectory, params=θ)\n    stop_condition = (land, step, event, when) -> false   # run until no events remain\n    ChronoSim.run(sim, init_physical!, stop_condition)\n    return trajectory.trajectory\nend\n\nThe stop condition always returns false; because the process is monotone, the simulation ends on its own when every point is occupied and no Spread event can be proposed. Note how θ is handed to the simulation through the params= keyword.\n\nusing ChronoSimExamples.LandSpread\ntraj = LandSpread.run_landspread(10)\n\n(Image: Saturation of a 40-point landscape)\n\nCumulative occupied points versus time for a single 40-point run. Because the process is monotone, the step curve only climbs, and it stops the moment the last point is occupied.","title":"Generating a trajectory"},{"category":"section","location":"models/landspread/usage/#Scoring-a-trajectory-at-a-parameter-value","page":"Running It","text":"landspread_likelihood(point_cnt; θ=SPREAD_THETA) records a trajectory and then evaluates its trace log-likelihood at θ:\n\nfunction landspread_likelihood(point_cnt; θ=SPREAD_THETA)\n    trajectory = run_landspread(point_cnt)          # generated at SPREAD_THETA\n    # ... convert to an event vector, drop the initialize event ...\n    rng = Xoshiro(9437294723)\n    land = Landscape(point_cnt, rng)\n    sim = SimulationFSM(land, [Spread];\n        sampler=NextReactionMethod(), key_type=Tuple,\n        step_likelihood=true, likelihood_eltype=eltype(θ), rng=rng)\n    return ChronoSim.trace_likelihood(sim, init_physical!, event_vector; params=θ).loglikelihood\nend\n\nTwo details make the θ seam work here:\n\nThe trajectory is generated at the default SPREAD_THETA, but the scoring uses whatever θ you pass. That is exactly the \"score the same trace at a different θ\" capability.\nlikelihood_eltype=eltype(θ) lets the accumulator hold whatever element type θ has — so a vector of ForwardDiff.Dual numbers flows through, and the gradient of the log-likelihood in the parameters comes out.\n\nLandSpread.landspread_likelihood(10)                 # score at SPREAD_THETA\nLandSpread.landspread_likelihood(10; θ=[0.2, 1.0])   # score the same trace elsewhere\n\n(Image: Trace log-likelihood swept over θ)\n\nScoring one recorded trace at a grid of parameter values. Sweeping either the scale coefficient θ[1] or the distance exponent θ[2] traces a smooth log-likelihood curve; the dashed line marks the generating SPREAD_THETA. This is the θ seam in one picture — generate once, score at many θ.","title":"Scoring a trajectory at a parameter value"},{"category":"section","location":"models/landspread/usage/#The-tests","page":"Running It","text":"The tests in test/test_landspread.jl are short but pointed. Their header records why they exist:\n\nLandspread previously had no functional test, which is how a silent no-write bug ... left it running zero events without anyone noticing. These tests pin that the process actually spreads and that the θ seam evaluates the same trace at different parameters.\n\nThe first test pins the saturation behavior. A ten-point run must produce exactly ten trajectory entries — one initialize event plus nine spreads, since every non-seed point gets occupied exactly once:\n\ntraj = with_logger(ConsoleLogger(stderr, Logging.Warn)) do\n    LandSpread.run_landspread(10)\nend\n@test length(traj) == 10\n@test count(te -> te.event[1] == :Spread, traj) == 9\n\nThe second test pins the θ seam. It scores the same recorded trace at two different parameter vectors and asserts both are finite and that they differ — the defining property that an estimator relies on:\n\nll, ll_shifted = with_logger(ConsoleLogger(stderr, Logging.Warn)) do\n    (LandSpread.landspread_likelihood(10),\n     LandSpread.landspread_likelihood(10; θ=[0.2, 1.0]))\nend\n@test isfinite(ll)\n@test isfinite(ll_shifted)\n@test ll != ll_shifted\n\nThese two tests are the whole story for landspread: the process spreads and saturates, and its likelihood moves with θ. For a fuller record-and-differentiate workflow — including checking a gradient against a hand-derived analytic score — see the repair shop.","title":"The tests"},{"category":"section","location":"models/sirvillage/usage/#Running-the-SIR-Village-Model","page":"Running It","text":"","title":"Running the SIR Village Model"},{"category":"section","location":"models/sirvillage/usage/#The-entry-point","page":"Running It","text":"The hand-written model exposes run_sirvillage, which builds a small village and runs it:\n\nfunction run_sirvillage(; policy=ChronoSim.NoPolicy())\n    person_cnt, location_cnt = 10, 10\n    day_length, days = 1.0, 1.0\n    rng = Xoshiro(2938423)\n    physical = Village(person_cnt, location_cnt, day_length, rng)\n    included_transitions = [InitEvent, Travel, Infect, Recover, Reset, Mutate]\n    trajectory = TrajectorySave()\n    sim = SimulationFSM(physical, included_transitions;\n        rng=rng, observer=trajectory, policy=policy)\n    stop_condition = (physical, step_idx, event, when) -> when > days\n    ChronoSim.run(sim, InitEvent(), stop_condition)\n    return sim.when\nend\n\nTwo things distinguish this from the simpler models. First, the initializer is an actual event — InitEvent() is passed to ChronoSim.run in place of an initializer function, and it seeds the infection and the founding strains when it fires. Second, Mutate and Reset make the state grow and cycle at runtime, so a run exercises the dict-keyed strain set and the SIRS loop.\n\nusing ChronoSimExamples.SIRVillage\nSIRVillage.run_sirvillage()\n\nRunning the larger 30-person, 15-day village from the test and counting S/I/R states after every event gives the epidemic curve below. The infection sweeps the village in the first day, recovery builds up an immune pool, and waning (Reset) plus new strains keep infections circulating; the lower panel tracks the cumulative number of strains as Mutate fires.\n\n(Image: S, I, and R counts and cumulative strain count over a 15-day run)\n\nA 30-person, 10-location, 15-day run (Xoshiro(2938423)): Susceptible, Infectious, and Recovered counts over time, with the cumulative strain count below.\n\nThe derived twin has a matching run_sirvillage_derived() entry point (30 people, 10 locations, 30 days), with no observer or policy attached.","title":"The entry point"},{"category":"section","location":"models/sirvillage/usage/#Checking-invariants-while-you-run","page":"Running It","text":"As with the elevator, invariant checking plugs in through the policy keyword. The tests stack two policies so that a violation carries a replayable prefix:\n\nusing ChronoSim: PolicyStack, RecordSkeleton, CheckInvariants\npolicy = PolicyStack(RecordSkeleton(), CheckInvariants(SIRVillage))\nSIRVillage.run_sirvillage(policy=policy)\n\nRecordSkeleton is placed before CheckInvariants so that if an invariant fails, the recorded skeleton lets you replay the run up to the failing step.","title":"Checking invariants while you run"},{"category":"section","location":"models/sirvillage/usage/#The-test","page":"Running It","text":"The test in test/test_sirvillage.jl is a single smoke test, but a pointed one. Its header explains what it guards against:\n\nThe model had no smoke test (its \"unfinished\" state): strains was a fixed-extent ObservedVector whose Mutate push! throws FixedExtentError, so any run long enough to mutate crashed. With strains as a keyed ObservedDict the run must complete and actually fire events (including Mutate, which grows the strain set).\n\nThe test builds a 30-person, 10-location village, runs it for fifteen days under the recording-plus-invariants policy stack, and counts events with a custom observer:\n\nevent_cnt = Ref(0)\nfired = Set{Symbol}()\nobserver = function (physical, when, event, changed)\n    event_cnt[] += 1\n    push!(fired, clock_key(event)[1])\nend\n\npolicy = PolicyStack(RecordSkeleton(), CheckInvariants(SIRVillage))\nsim = SimulationFSM(physical, included; rng=rng, observer=observer, policy=policy)\nstop_condition = (p, step_idx, event, when) -> when > days\nwith_logger(ConsoleLogger(stderr, Logging.Warn)) do\n    ChronoSim.run(sim, SIRVillage.InitEvent(), stop_condition)\nend\n\nIts three assertions are the smoke signals:\n\n@test sim.when > 0.0                    # the run advanced\n@test event_cnt[] > 0                   # events actually fired\n@test physical.next_strain_id > 2       # a strain was born at runtime\n\nThe last one is the important regression guard: next_strain_id > 2 means a Mutate grew the strain set beyond the founding strain — the exact path that used to throw FixedExtentError before strains became an ObservedDict. Passing the run under CheckInvariants also means every one of the model's invariants held after each of its events.\n\nThis is a smoke and regression test, not a statistical validation of the epidemic dynamics. It confirms the machinery runs — movement, infection, recovery, waning, and mutation all fire, and the state stays consistent throughout.","title":"The test"},{"category":"section","location":"models/reliability/usage/#Running-the-Reliability-Model","page":"Running It","text":"","title":"Running the Reliability Model"},{"category":"section","location":"models/reliability/usage/#The-entry-point","page":"Running It","text":"The hand-written model exposes run_reliability(days), which builds a 15-machine fleet with a crew cap of 10 and runs it for the requested number of days:\n\nfunction run_reliability(days)\n    physical = IndividualState(15, 10)\n    included_transitions = [StartDay, EndDay, Break, Repair]\n    trajectory = TrajectorySave()\n    sim = SimulationFSM(\n        physical, included_transitions;\n        rng=Xoshiro(2947223), observer=trajectory,\n        sampler=NextReactionMethod(), key_type=Tuple)\n    initializer = (physical, when, rng) -> initialize!(physical, rng)\n    stop_condition = (physical, step_idx, event, when) -> when > days\n    ChronoSim.run(sim, initializer, stop_condition)\n    return sim.when\nend\n\nThe stop_condition sees each chosen event before it fires, so the run stops cleanly at the requested day boundary rather than partway through an event.\n\nTo run it:\n\nusing ChronoSimExamples.ReliabilitySim\nReliabilitySim.run_reliability(10.0)   # ten simulated days\n\n(Image: Fleet state occupancy over ten days)\n\nReconstructing the run with a state-counting observer shows the daily rhythm: each morning StartDay lifts the working count to the crew cap of 10, machines trickle back to ready as they finish, and a growing tail sits broken awaiting repair.\n\nThe derived twin has a matching entry point, run_reliability_derived(days), which is identical except that it attaches no observer.","title":"The entry point"},{"category":"section","location":"models/reliability/usage/#The-tests","page":"Running It","text":"The dedicated tests in test/test_reliability.jl are intentionally minimal — two smoke checks with no assertions on the numbers produced.\n\nThe first confirms the state constructs:\n\n@testset \"Reliability smoke\" begin\n    using ChronoSimExamples.ReliabilitySim\n    is = ReliabilitySim.IndividualState(15, 10)\nend\n\nThe second runs the whole simulation end to end, adjusting the log level for CI:\n\n@testset \"Reliability run\" begin\n    using ChronoSimExamples.ReliabilitySim\n    ci = continuous_integration()\n    log_level = ci ? Logging.Error : Logging.Debug\n    with_logger(ConsoleLogger(stderr, log_level)) do\n        run_reliability(10)\n    end\nend\n\nPassing without error means the fleet ran for ten days: machines went to work, some finished their day, some broke down and were repaired, and the aging clocks behaved. There is no assertion on trajectory contents here.","title":"The tests"},{"category":"section","location":"models/reliability/usage/#Where-the-model-is-checked-more-rigorously","page":"Running It","text":"The substantive checking of this model does not live in test_reliability.jl. It happens in the tests that compare the hand-written model against its derived twin, which is why the two share clock keys:\n\ntest/test_differential.jl runs ReliabilitySim and ReliabilityDerivedSim from the same seed and asserts their trajectories match step for step — the guarantee that hand-written and derived triggers are equivalent.\ntest/test_overapprox.jl runs both and compares aggregate statistics, and measures the extra proposal cost the derived generators pay for their over-approximation.\n\nIf you want a worked example of the θ seam that reliability deliberately avoids, see the landspread model; for the record, check, and differentiate workflow, see the repair shop.","title":"Where the model is checked more rigorously"},{"category":"section","location":"models/repairshop/likelihood/#The-Likelihood-Workflow","page":"The Likelihood Workflow","text":"The repair shop's main job is to demonstrate a three-step pattern that ChronoSim's record/replay machinery exists to support:\n\nRecord one run under a RecordMinimal policy, capturing a MinimalRecord.\nCheck the record with effect_check — replaying it must reproduce the forward log-likelihood exactly, === and not ≈.\nDifferentiate the trace log-likelihood in θ with ForwardDiff, evaluating at a θ the forward run never saw.\n\nThe example then verifies the resulting gradient against a hand-derived analytic score, so this is an oracle-checked demonstration rather than a demo that merely runs.","title":"The Likelihood Workflow"},{"category":"section","location":"models/repairshop/likelihood/#Recording-a-run","page":"The Likelihood Workflow","text":"record_repair_shop runs the simulation once under the RecordMinimal policy and returns the captured record together with the policy:\n\nfunction record_repair_shop(θ; machine_cnt=3, horizon=20.0, seed=90210)\n    policy = RecordMinimal(; initializer=init!)\n    sim = SimulationFSM(Shop(machine_cnt), _events();\n        step_likelihood=true, policy=policy, params=θ,\n        sampler=NextReactionMethod(), key_type=Tuple)\n    stop_condition = (shop, step, event, when) -> when > horizon\n    ChronoSim.run(sim, init!, stop_condition)\n    return (record=minimal_record(policy; horizon=horizon), policy=policy)\nend\n\nA MinimalRecord holds just enough to replay: the initializer, the ordered sequence of (clock key, firing time) pairs, the horizon, and a fire_random flag recording whether any fire! body drew randomness. For the repair shop that flag is false, which is why replay is deterministic.","title":"Recording a run"},{"category":"section","location":"models/repairshop/likelihood/#Checking-the-record-exactly","page":"The Likelihood Workflow","text":"effect_check replays the record through trace_likelihood and demands exact floating-point equality with the forward log-likelihood. Because forward execution and trace evaluation share one code path, any difference is a bug, not roundoff — so the check is ===, not ≈:\n\nfactory = () -> SimulationFSM(Shop(machine_cnt), RepairShop._events();\n    step_likelihood=true, params=θ,\n    sampler=NextReactionMethod(), key_type=Tuple)\nres = ChronoSim.effect_check(factory, RepairShop.init!, policy)\n\n@test res.applicable\n@test res.passed\n@test res.forward === res.replay   # exact Float64 identity, not isapprox\n@test isfinite(res.forward)","title":"Checking the record exactly"},{"category":"section","location":"models/repairshop/likelihood/#Differentiating-the-trace-log-likelihood","page":"The Likelihood Workflow","text":"Scoring is done by rebuilding a fresh evaluation simulation and calling trace_likelihood with the record and a chosen θ. The likelihood_eltype=eltype(θ) keyword lets dual numbers accumulate without being truncated to Float64, and censor=true adds the finite-horizon survival tail:\n\nfunction repair_shop_loglik(θ, record; machine_cnt=3)\n    sim = SimulationFSM(Shop(machine_cnt), _events();\n        step_likelihood=true, params=θ, likelihood_eltype=eltype(θ),\n        sampler=NextReactionMethod(), key_type=Tuple)\n    return trace_likelihood(sim, init!, record; params=θ, censor=true).loglikelihood\nend\n\nrepair_shop_score(θ, record; machine_cnt=3) =\n    ForwardDiff.gradient(p -> repair_shop_loglik(p, record; machine_cnt), θ)\n\n(Image: Trace log-likelihood versus breakdown rate with the ForwardDiff tangent)\n\nOne record from record_repair_shop([0.3, 1.0]) is fixed; sweeping the breakdown rate θ₁ traces the trace log-likelihood, and the dashed line is the tangent whose slope is repair_shop_score(θ)[1] — ForwardDiff recovers the exact slope of the curve.\n\nThe trace is fixed; only the evaluation parameters vary. That makes the score a deterministic function of (θ, record) — the same property landspread demonstrates, applied here to a differentiable gradient.\n\nThe whole workflow is wrapped up in repair_shop_demo, which records, checks, and scores, printing the firing count, the log-likelihood, and the score.","title":"Differentiating the trace log-likelihood"},{"category":"section","location":"models/repairshop/likelihood/#The-analytic-oracle","page":"The Likelihood Workflow","text":"Because both clocks are exponential, the trace log-likelihood has a closed form. With n_break breakdowns, n_repair repairs, and exposures E_run (total machine-time spent running) and E_down (spent down):\n\nloglik = n_break·log(λ_b) + n_repair·log(λ_r) − λ_b·E_run − λ_r·E_down\n\nwhich differentiates to\n\ndloglik/dλ_b = n_break/λ_b − E_run\ndloglik/dλ_r = n_repair/λ_r − E_down\n\nThe test in test/test_repairshop.jl implements exactly this in analytic_score, walking the record's firings to accumulate the exposures and counts and adding the censoring tail from the last firing to the horizon.","title":"The analytic oracle"},{"category":"section","location":"models/repairshop/likelihood/#The-tests","page":"The Likelihood Workflow","text":"test/test_repairshop.jl is thorough — three testsets built on the hand-derived oracle.\n\nThe first pins that the recorded trajectory is usable and reproducible: it draws no randomness, has the expected horizon, is long enough to be worth testing, and comes out identical from the same seed:\n\n(; record, policy) = RepairShop.record_repair_shop(θ; machine_cnt=3, horizon=20.0, seed=90210)\n@test record.fire_random == false\n@test record.horizon == 20.0\n@test length(record.firings) > 10\nagain = RepairShop.record_repair_shop(θ; machine_cnt=3, horizon=20.0, seed=90210)\n@test again.record.firings == record.firings\n\nThe second is the effect_check example shown above — replay reproduces the forward log-likelihood to exact Float64 identity.\n\nThe third checks the ForwardDiff score against the analytic oracle, and then re-scores the same fixed trace at a different θ and checks that too — showing the score is correct away from the parameters that generated the trace, which is the property estimators rely on:\n\ng = RepairShop.repair_shop_score(θ, record; machine_cnt)\n@test isapprox(g, analytic_score(θ, record, machine_cnt); rtol=1e-9)\n\nθ2 = [0.5, 0.7]\ng2 = RepairShop.repair_shop_score(θ2, record; machine_cnt)\n@test isapprox(g2, analytic_score(θ2, record, machine_cnt); rtol=1e-9)\n\nNote the tolerance: rtol=1e-9. The ForwardDiff gradient and the hand-derived formula agree to nine digits, because they are computing the same thing two ways.\n\nThe gradient estimators page covers the companion example, which moves from differentiating a fixed trace to estimating the gradient of an expectation over trajectories.","title":"The tests"},{"category":"section","location":"models/elevator/model/#The-Elevator-Model","page":"The Model","text":"The elevator example is a continuous-time port of a well-known TLA+ specification. It models a building with several elevator cars, a set of people who travel between floors, and a simple dispatcher that decides which car answers which call.","title":"The Elevator Model"},{"category":"section","location":"models/elevator/model/#Where-it-comes-from","page":"The Model","text":"The model is a Julia/ChronoSim port of the Elevator module from the tlaplus/Examples collection. The header of src/elevator/elevator.jl records the provenance:\n\n# This implements a set of elevators and people interacting.\n# The spec comes from https://github.com/tlaplus/Examples which has\n# elevator.tla, the MODULE Elevator.\n#\n# Run the .tla with `java -jar tla2tools.jar -config Elevator.cfg Elevator.tla`.\n\nThe original specification is kept in the repository at src/elevator/Elevator.tla as provenance documentation. Its own module comment describes the design:\n\nThis spec describes a simple multi-car elevator system. The actions in this spec are unsurprising and common to all such systems except for DispatchElevator, which contains the logic to determine which elevator ought to service which call. The algorithm used is very simple and does not optimize for global throughput or average wait time.\n\nThe Julia model mirrors the TLA+ variables, its nine actions, and its type and safety invariants. The important difference is that the TLA+ spec is untimed — it describes which actions are legal, not when they happen — while the ChronoSim port draws a waiting time for every action from an Exponential(1.0) clock, so the model becomes a continuous-time stochastic simulation.\n\nnote: Two dead files\nsrc/elevator/elevator_tla_example.jl is a stale demo that references APIs the model no longer exposes; it will not run. The TLA+/TLC trace glue was also retired to attic/elevatortla.jl. Model-checking validation now goes through Quint (test/test_quint.jl). Do not treat either as a live example.","title":"Where it comes from"},{"category":"section","location":"models/elevator/model/#What-it-models","page":"The Model","text":"A building with a fixed number of floors, a fixed set of people, and a fixed set of elevator cars. People decide to travel to another floor, press a hall call, board a car whose doors open in their direction, ride to their destination, and leave. Each car moves up or down one floor at a time, opens and closes its doors, and tracks the destination buttons its passengers have pressed. A dispatcher assigns the closest stationary or already-approaching car to each active call.","title":"What it models"},{"category":"section","location":"models/elevator/model/#State","page":"The Model","text":"The state is declared with ChronoSim's ObservedState macros, so the framework observes every read a precondition makes and every write a firing makes. There are four keyed record types and one top-level observed state.\n\nA direction enum carries a content-based hash so that clock keys built from it reproduce across processes and modules — a detail that matters when the hand-written and derived twins compare trajectories:\n\n@enum ElevatorDirection Up Down Stationary\nBase.hash(x::ElevatorDirection, h::UInt) = hash(Symbol(x), h)\n\nA Person is keyed by an integer id:\n\n@keyedby Person Int64 begin\n    location::Int64      # floor the person is on; 0 while riding\n    destination::Int64   # target floor\n    elevator::Int64      # elevator id they are in; 0 if not riding\n    waiting::Bool        # waiting for a called elevator\nend\n\nAn ElevatorCall is keyed by a (floor, direction) pair and records whether that hall call is currently active:\n\n@keyedby ElevatorCall Tuple{Int64,ElevatorDirection} begin\n    requested::Bool\nend\n\nAn Elevator is keyed by an integer id:\n\n@keyedby Elevator Int64 begin\n    floor::Int64\n    direction::ElevatorDirection      # Up, Down, or Stationary\n    doors_open::Bool\n    buttons_pressed::Set{Int64}       # destination floors selected inside the car\nend\n\nThe whole building is the observed physical state:\n\n@observedphysical ElevatorSystem begin\n    person::ObservedVector{Person,Member}\n    calls::ObservedDict{Tuple{Int64,ElevatorDirection},ElevatorCall,Member}\n    elevator::ObservedVector{Elevator,Member}\n    floor_cnt::Int64\nend\n\nThe constructor ElevatorSystem(person_cnt, elevator_cnt, floor_cnt) starts everyone on floor 1, creates an inactive call for every (floor, direction) pair, and parks every car stationary on floor 1 with its doors closed. A Base.show method pretty-prints the building floor by floor, marking cars, active up/down calls, and the people on each floor — useful when watching a run.","title":"State"},{"category":"section","location":"models/elevator/model/#Events","page":"The Model","text":"There are exactly nine event types, matching the nine TLA+ actions. Every event is a struct <: SimEvent, uses the timing enable(...) = (Exponential(1.0), when), and defines three things: a set of triggers (which state changes propose the event), a precondition (whether it may fire), and fire! (its effect).\n\nEvent Fires when Effect\nPickNewDestination(person) an idle person on a floor chooses a random destination floor different from the current one\nCallElevator(person) a person's destination differs from their floor and they are not yet waiting marks the person waiting and raises the hall call for their direction\nOpenElevatorDoors(elevator) a car reaches a floor with a matching call or a pressed button opens the doors, clears the button and the call\nEnterElevator(elevator) doors open with people waiting to go that direction boards every matching waiter and presses their destination buttons\nExitElevator(elevator) doors open with a passenger whose destination is this floor lets those passengers out\nCloseElevatorDoors(elevator) doors are open and nobody needs to board or exit closes the doors\nMoveElevator(elevator) doors closed, a direction set, and no reason to stop here moves the car one floor\nStopElevator(elevator) doors closed at a boundary floor with no reason to open sets the car stationary\nDispatchElevator(floor, direction) an active call with a stationary or approaching car available steers the closest suitable car toward the call\n\n(Image: Elevator car service cycle and rider lifecycle) The nine events split into two coupled loops: a car cycles through dispatch, motion, and door handling, while a rider picks a destination, calls, boards, rides, and exits.\n\nThree of these preconditions are worth noting because they show ChronoSim's more advanced features:\n\nCloseElevatorDoors calls the preconditions of EnterElevator and ExitElevator inside its own precondition — it may close the doors only when neither of those events could fire. This \"precondition recursion\" keeps the rules consistent instead of duplicating them.\nStopElevator similarly calls OpenElevatorDoors' precondition: a car stops only when it is at a boundary floor and is not about to open its doors.\nMoveElevator encodes the TLA+ rule that a car may pass a call it could service only when another car is able to service that call.\n\nSmall helpers such as get_direction, can_service_call, and people_waiting are marked @fragment so ChronoSim can inline their bodies into the static analysis when a precondition passes state into them.","title":"Events"},{"category":"section","location":"models/elevator/model/#Invariants","page":"The Model","text":"The model declares eight @invariant blocks — pure boolean checks over the physical state — that mirror the TLA+ type and safety invariants. Among them: a person is either on a floor or in an elevator but never both (\"person location xor elevator\"), every pressed button corresponds to a real passenger (\"pressed button has passenger\"), and there are no active calls without a waiting person (\"no ghost calls\"). Running under the CheckInvariants policy verifies all of them after every fired event; see Running the Elevator.","title":"Invariants"},{"category":"section","location":"models/elevator/model/#The-derived-twin","page":"The Model","text":"The module ElevatorDerivedExample in src/elevator/elevator_derived.jl is a parallel copy of the model in which the event triggers are derived from the precondition bodies instead of being written by hand. In the hand-written model, each event declares its triggers explicitly with @conditionsfor / @reactto changed(...) blocks. In the derived twin those blocks are gone; each precondition is marked @precondition, and ChronoSim statically analyzes it — every state field the precondition reads becomes a trigger.\n\nBecause both twins key their clocks by nameof(type), a hand-written MoveElevator and a derived MoveElevator share the same clock key, so the two versions produce identical trajectories from the same seed. That equivalence is more than a curiosity: several fixes in the hand-written model — the location != 0 guard added to CallElevator, and the extra triggers added to StopElevator — were discovered by running the two twins side by side and finding where their trajectories diverged. The debugging tools page shows those comparisons in action.","title":"The derived twin"},{"category":"section","location":"models/landspread/model/#The-Landspread-Model","page":"The Model","text":"Landspread simulates a contagion spreading across a landscape: a set of points scattered on a plane, where an occupied point can spread to an unoccupied one at a rate that depends on the distance between them. It is the repository's canonical demonstration of the θ (parameter) seam — the four-argument form of enable that lets one recorded trajectory be re-scored at different parameter values.\n\n(Image: Spread cascade across a 40-point landscape)\n\nThe signature figure: points placed on the unit square, colored by the time each was infected, with an arrow from source to destination for every spread. The star marks the seed (point 1), and the cascade fans out from it.","title":"The Landspread Model"},{"category":"section","location":"models/landspread/model/#Where-it-comes-from","page":"The Model","text":"The header describes the process directly:\n\nThis will simulate spread across a landscape as a process on a graph. There are points on a 2D plane and distances among those points. The rate of spread from one point to the next depends on the distance. You start it somewhere and observe it later.\n\nThe intellectual inspiration is disease spread across geographic sites. The companion file src/landspread/genspread.jl frames the problem as inferring infection events from partial observations, and the repository carries a reference PDF, rjmcmc_chagas.pdf, on reversible-jump MCMC for Chagas disease.\n\nnote: genspread.jl is a sketch, not a runnable example\ngenspread.jl is a standalone RJMCMC prototype written against the Gen probabilistic-programming library. It is not part of the compiled module (ChronoSimExamples.jl does not include it) and it references a landspread_log_likelihood function that does not exist — the real one is landspread_likelihood. Read it as an illustration of how the θ-seam likelihood would feed a Bayesian inference layer, not as working code.","title":"Where it comes from"},{"category":"section","location":"models/landspread/model/#What-it-models","page":"The Model","text":"N points are placed at random on the unit square. A binary occupation state spreads from point to point: an occupied point (\"mark = 1\") can infect an unoccupied one (\"mark = 0\"), and the waiting time for that spread depends on the Euclidean distance between them. The landscape is fully connected — every point can in principle spread to every other — with distance only modulating the timing. The process is monotone: a point never becomes unoccupied again, so a run seeded at one point eventually saturates the whole landscape.","title":"What it models"},{"category":"section","location":"models/landspread/model/#State","page":"The Model","text":"Landspread is unusual among these examples in that it does not use the @observedphysical macro. Instead it tracks reads and writes by hand with @obsread and @obswrite, which is why the state carries its own observation sets:\n\nstruct Landscape <: ObservedPhysical\n    mark::Array{Int,1}            # 0 = unoccupied, 1 = occupied\n    distance::Array{Float64,2}    # N×N Euclidean distance matrix\n    obs_read::Set{Tuple}          # addresses read this step\n    obs_modified::Set{Tuple}      # addresses written this step\nend\n\nThe constructor Landscape(N, rng) draws N random locations on the plane, fills the distance matrix, and starts every point unoccupied.","title":"State"},{"category":"section","location":"models/landspread/model/#The-single-event","page":"The Model","text":"There is one event type, Spread, with a source and a destination:\n\nstruct Spread <: SimEvent\n    source::Int\n    destination::Int\nend\n\nIts generator reacts to a change in any point's mark: when point i becomes occupied, it proposes Spread(i, j) for every other point j. Its precondition admits the event only when the source is occupied and the destination is empty:\n\n@guard function precondition(evt::Spread, land)\n    source_mark = @obsread land.mark[evt.source]\n    dest_mark = @obsread land.mark[evt.destination]\n    return evt.source != evt.destination && source_mark == 1 && dest_mark == 0\nend\n\nFiring marks the destination occupied — written as two statements, a plain assignment followed by @obswrite applied only to the access:\n\n@fire function fire!(evt::Spread, land, when, rng)\n    land.mark[evt.destination] = 1\n    @obswrite land.mark[evt.destination]\nend\n\nwarning: A historical bug worth understanding\nWriting this as @obswrite land.mark[evt.destination] = 1 would drop the right-hand side: the macro records the address but evaluates only the access, so the write never happens. That exact mistake once made landspread run zero events while appearing to run fine. The usage page shows the test that now guards against it.\n\nThe initializer seeds point 1 (mark[1] = 1), which produces the first changed(mark[1]) and bootstraps the whole cascade.","title":"The single event"},{"category":"section","location":"models/landspread/model/#The-θ-seam","page":"The Model","text":"The reason landspread exists is its enable, which takes the four-argument form and reads its coefficients from the parameter vector θ rather than hard-coding them:\n\nconst SPREAD_THETA = [0.1, 1.2]\n\nfunction enable(evt::Spread, land, θ, when)\n    dist = land.distance[evt.source, evt.destination]\n    scale = θ[1] * dist^θ[2]\n    return (Weibull(2, scale), when)\nend\n\nHere θ[1] is the scale coefficient and θ[2] the distance exponent of the Weibull spread clock; a larger distance yields a larger scale and therefore slower spread. The header explains why this matters:\n\nReading the spread coefficients from θ instead of hard-coding them means an estimator can re-evaluate this same trace at a different θ — including a vector of ForwardDiff duals — through the params= keyword of trace_likelihood, with no module-global state.\n\nThe parameter vector enters the simulation through the params= keyword of the SimulationFSM constructor (for generating a trajectory) and the params= keyword of trace_likelihood (for scoring one). Because nothing lives in module-global state, the same recorded trajectory can be scored at any θ — the property that makes gradient-based model fitting possible. Models that still use the three-argument enable keep working; the four-argument default forwards to them, as the reliability model deliberately shows.","title":"The θ seam"},{"category":"section","location":"dwell/#Characterizing-Travel","page":"Characterizing Travel","text":"I want to make individuals that travel a city. I want to give each individual a home, work, and other places to go. Home and work should get some solid dwell times, which is the average time spent there. Other places might be visitied less than once a day. I'd like to assign each location a dwell time and a fraction of their total time spent there. And this should be done as draws from a distribution.\n\nWe should start with a Markov chain of places to visit. Choose a finite set of places. Each place will have a dwell time D_i. Each place will be assigned a fraction of total time spent, pi_i. We will assume that the hazard for travel from any i to j is Weibull-distributed. We'll parametrize the Weibull in a way that's friendly to simulation so the hazard rate is lambda_ij(t)=alpha_ijt where alpha_ij is a constant.\n\nIn this case, we can define A_i=sum_jne ialpha_ij the survival is S_i(t)=exp(A_it^22) the dwell time is D_i = sqrtfracpi2A_i Equivalently, A_i=2D_i^2pi. Here, this is the constant pi not the fraction of time pi_ij. That means we are putting a number on the sum of all hazard rates out of a location.\n\nIf we think about the embedded Markov chain, then the transition probability from i to j is P_ij=fracalpha_ijsum_kne ialpha_ik The eigenvalues of that matrix are the fraction of transitions that land in each state for the embedded matrix. The eigenvalues, of course, are pP=p with sump_i=1. From this, the fraction of time in each state is pi_i=fracp_iD_isum_kp_kD_k This gives us an implicit set of equations for our free parameters, the alpha_ij. Given that the dwell times set the sum of outgoing rates, we can think of this as finding the P_ij in the embedded Markov chain.\n\nHow many free parameters are there? There are n locations, each of which has a rate to the other locations, which are n-1, so there are n(n-1) parameters. We have applied only n dwell time constraints and n-1 fractional constraints, where the -1 is because the fractions always add to 1. That leaves n^2-n-2n+1=n^2-3n+1.\n\nWe are going to deal with the excess of parameters by choosing a maximum entropy solution.\n\nFor each state i, the maximum entropy solution is the set of outgoing transition probabilities P_ij that maximizes the information entropy S_i=-sum_jP_ijln P_ij, subject to our constraints (the eigenvalues pP=p). This is a roundabout way of waying that every row of our transition matrix P_ij should be the same. That is, when someone leaves any location, their probability of going to any other location is the same.\n\nThat means we just need to invert to find: p_i=fracpi_iD_isum_k pi_kD_k Then every alpha_ij=A_ip_j.","title":"Characterizing Travel"},{"category":"section","location":"models/repairshop/model/#The-Repair-Shop-Model","page":"The Model","text":"The repair shop is a deliberately tiny model — a handful of machines that break down and get repaired — whose purpose is not the dynamics but the machinery around the dynamics. It is the repository's demonstration of ChronoSim's record-and-replay workflow and of differentiating a trajectory's likelihood with respect to its parameters.","title":"The Repair Shop Model"},{"category":"section","location":"models/repairshop/model/#Where-it-comes-from","page":"The Model","text":"There is no external citation; it is a purpose-built demonstration vehicle. The header states the intent plainly:\n\nThe model: machine_cnt machines run until they break down and are repaired. Both clocks are exponential; θ = [breakdown rate, repair rate]. Exponential clocks keep the analytic score simple enough to verify by hand in the test suite, which is what makes this example an oracle-checked demonstration rather than a demo that merely runs.\n\nStructurally it is the classic queueing-theory \"machine repair problem,\" though the code never uses that name — and, importantly, there is no limited repairman resource. Every down machine is repaired concurrently, so the model is really machine_cnt independent two-state Markov chains: each machine goes up →(λ) down →(μ) up. That independence is exactly what makes a closed-form answer available to check the estimators against.","title":"Where it comes from"},{"category":"section","location":"models/repairshop/model/#What-it-models","page":"The Model","text":"A shop of machine_cnt machines (default 3). Each machine is either running or down. A running machine breaks down after an Exponential(θ[1]) delay; a down machine is repaired after an Exponential(θ[2]) delay and returns to running. The parameter vector is θ = [breakdown rate, repair rate].","title":"What it models"},{"category":"section","location":"models/repairshop/model/#State","page":"The Model","text":"The state is minimal — one condition field per machine:\n\n@enum MachineCondition running down\n\n@keyedby Machine Int64 begin\n    condition::MachineCondition\nend\n\n@observedphysical Shop begin\n    machines::ObservedVector{Machine,Member}\nend\n\nShop(machine_cnt) allocates the machines and starts each one running.","title":"State"},{"category":"section","location":"models/repairshop/model/#Events","page":"The Model","text":"Two events, each keyed by a machine index. Both take the four-argument θ-seam form of enable, building their exponential clock from the caller-supplied θ:\n\n(Image: One machine as a two-state chain)\n\nEach machine is an independent two-state chain: running breaks down at rate θ₁ and down is repaired at rate θ₂.\n\nBreakdown(machine) — enabled while the machine is running:\n\nenable(evt::Breakdown, shop, θ, when) = (Exponential(inv(θ[1])), when)\n\nfunction fire!(evt::Breakdown, shop, when, rng)\n    shop.machines[evt.machine].condition = down\nend\n\nRepair(machine) — enabled while the machine is down:\n\nenable(evt::Repair, shop, θ, when) = (Exponential(inv(θ[2])), when)\n\nfunction fire!(evt::Repair, shop, when, rng)\n    shop.machines[evt.machine].condition = running\nend\n\nNeither fire! draws any randomness — the events only flip a condition. That matters for the record-and-replay workflow: because nothing in a firing consumes the random stream, a recorded trajectory replays deterministically. The initializer writes every machine's condition to running, and those writes propose the first Breakdown events.","title":"Events"},{"category":"section","location":"models/repairshop/model/#Why-θ-is-read-from-a-parameter-vector","page":"The Model","text":"The load-bearing design point is that each event builds its distribution from the θ passed in, not from a constant baked into the code. When θ is an ordinary vector of floats you get an ordinary simulation. When θ is a vector of ForwardDiff.Dual numbers, inv(θ[1]) promotes and the dual flows into the Exponential — so evaluating the trajectory's likelihood at a dual-valued θ yields not just the log-likelihood but its gradient in the parameters, with no global state changing between evaluations. That is the foundation of the two pages that follow:\n\nThe likelihood workflow — record a run, check the replay against the forward log-likelihood exactly, then differentiate the trace log-likelihood with ForwardDiff and compare to a hand-derived analytic score.\nGradient estimators — the companion repairshop_gradients.jl runs ClockGradients' whole estimator family against one closed-form oracle.","title":"Why θ is read from a parameter vector"},{"category":"section","location":"#ChronoSimExamples","page":"Home","text":"Documentation for ChronoSimExamples.\n\nThe repository README describes what each example demonstrates, including the θ (parameter) seam, trajectory records with the exact-replay effect check, and the repair-shop differentiation workflow. Users migrating a model from CompetingClocks 0.3 to 0.4 should read MIGRATION.md at the repository root, which orders the required changes by likelihood with before/after snippets.","title":"ChronoSimExamples"},{"category":"section","location":"#The-models","page":"Home","text":"Each example has its own section: a first page presenting the model — where it comes from, what it represents, and the state and events it defines — and one or more further pages on how to work with it, drawn from the model's tests.\n\nElevator — a continuous-time port of a TLA+ multi-car elevator spec, and the main fixture for ChronoSim's debugging tools.\nReliability — a fleet of machines that age and fail; the suite's evidence that the pre-θ-seam three-argument enable still works.\nRepair Shop — machines breaking and being repaired, used to demonstrate record/replay, exact effect checks, and differentiable trajectory likelihoods with the ClockGradients estimator family.\nLandspread — a contagion spreading across a landscape; the canonical demonstration of the θ (parameter) seam.\nSIR Village — an individual-based SIRS epidemic with movement and an evolving pathogen.\n\nSeveral models come in pairs — a hand-written module and a \"derived twin\" whose event triggers ChronoSim generates from the preconditions — and the test suite asserts the two produce identical trajectories from the same seed.\n\n","title":"The models"}]
}
