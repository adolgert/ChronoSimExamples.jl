name: CI
on:
  push:
    branches:
      - main
      # Long-running integration branch for the generator-derivation work; the
      # differential/coverage/over-approximation evaluations must run in CI.
      - dev/generator
    tags: ['*']
  pull_request:
  workflow_dispatch:
concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}
jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    permissions: # needed to allow julia-actions/cache to proactively delete old caches that it has created
      actions: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        version:
          # The Manifest tracks working-tree checkouts of ChronoSim and
          # CompetingClocks resolved under current stable; 'pre' adds noise
          # without guarding anything the dev branches promise.
          - '1'
        os:
          - ubuntu-latest
        arch:
          - x64
    steps:
      - uses: actions/checkout@v6
      # The Manifest points at sibling working-tree checkouts
      # (/home/adolgert/dev/...), which do not exist on a runner. Check the
      # siblings out and re-develop them by path before building.
      - uses: actions/checkout@v6
        with:
          repository: adolgert/ChronoSim.jl
          ref: main
          path: deps/ChronoSim.jl
      - uses: actions/checkout@v6
        with:
          repository: adolgert/CompetingClocks.jl
          path: deps/CompetingClocks.jl
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
      - uses: julia-actions/cache@v2
      - name: Develop sibling dependencies
        shell: julia --project=. --color=yes {0}
        run: |
          using Pkg
          Pkg.develop([
            PackageSpec(path="deps/CompetingClocks.jl"),
            PackageSpec(path="deps/ChronoSim.jl"),
          ])
          Pkg.instantiate()
      - uses: julia-actions/julia-runtest@v1
  # Phase 4: compile the models to Quint and run the toolchain-gated checker tests
  # (typecheck, `quint run`, bounded Apalache trace validation + the mutation test).
  # A SEPARATE job — it does NOT gate the `test` job above. Versions are pinned per
  # ChronoSim.jl's quint_spike/VERSIONS.md.
  model-check:
    name: Model check (Quint + Apalache)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/checkout@v6
        with:
          repository: adolgert/ChronoSim.jl
          ref: main
          path: deps/ChronoSim.jl
      - uses: actions/checkout@v6
        with:
          repository: adolgert/CompetingClocks.jl
          path: deps/CompetingClocks.jl
      - uses: actions/setup-node@v4          # VERSIONS.md: Node v22.16.0
        with:
          node-version: '22'
      - uses: actions/setup-java@v4          # VERSIONS.md: Temurin 17.0.19 (sets JAVA_HOME)
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Install pinned Quint
        run: npm install --prefix quint-tools @informalsystems/quint@0.32.0
      - uses: actions/cache@v4               # ~/.quint holds apalache-dist-0.56.1
        with:
          path: ~/.quint
          key: apalache-0.56.1
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1'
      - uses: julia-actions/cache@v2
      - name: Develop sibling dependencies
        shell: julia --project=. --color=yes {0}
        run: |
          using Pkg
          Pkg.develop([
            PackageSpec(path="deps/CompetingClocks.jl"),
            PackageSpec(path="deps/ChronoSim.jl"),
          ])
          Pkg.instantiate()
      # Deliberately re-runs the FULL suite (not just the quint testsets):
      # selection machinery isn't worth the maintenance while the whole run fits
      # the 30-minute timeout; the quint testsets activate via CHRONOSIM_QUINT.
      - name: Run the model-checking tests
        run: julia --project=. test/runtests.jl
        env:
          CHRONOSIM_QUINT: ${{ github.workspace }}/quint-tools
  docs:
    name: Documentation
    # The docs environment instantiates the path-dev Manifest, which only
    # resolves on main's registered dependency graph; the integration branch
    # gets the test job alone.
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      actions: write # needed to allow julia-actions/cache to proactively delete old caches that it has created
      contents: write
      statuses: write
    steps:
      - uses: actions/checkout@v6
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1'
      - uses: julia-actions/cache@v2
      - name: Configure doc environment
        shell: julia --project=docs --color=yes {0}
        run: |
          using Pkg
          Pkg.develop(PackageSpec(path=pwd()))
          Pkg.instantiate()
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-docdeploy@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}
      - name: Run doctests
        shell: julia --project=docs --color=yes {0}
        run: |
          using Documenter: DocMeta, doctest
          using ChronoSimExamples
          DocMeta.setdocmeta!(ChronoSimExamples, :DocTestSetup, :(using ChronoSimExamples); recursive=true)
          doctest(ChronoSimExamples)
